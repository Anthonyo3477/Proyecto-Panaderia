<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Panadería</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Archivo de estilos personalizados -->
    <link rel="stylesheet" href="/css/panaderia.css">
</head>

<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg bg-light fixed-top">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Panadería Don Juanito</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item"><a class="nav-link active" href="/">Inicio</a></li>
                    <li class="nav-item"><a class="nav-link" href="/panaderia">Panadería</a></li>
                    <li class="nav-item"><a class="nav-link" href="/reposteria">Repostería</a></li>
                    <li class="nav-item"><a class="nav-link" href="/carro">Carro</a></li>
                    <li class="nav-item"><a class="nav-link" href="/registro">Registro</a></li>
                    <li class="nav-item"><a class="nav-link" href="/AgregarProducto">Agregar Productos</a></li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Contenido principal -->
    <main class="container mt-5 pt-5">
        <h1 class="text-center">Panadería</h1>
        <p class="text-center">Explora nuestros productos de panadería</p>

        <!-- Tabla de productos -->
        <div class="table-responsive mt-4">
            <table class="table table-bordered table-hover align-middle">
                <thead class="table-light text-center">
                    <tr>
                        <th>ID</th>
                        <th>Nombre</th>
                        <th>Clasificación</th>
                        <th>Descripción</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (productos.length> 0) { %>
                        <% productos.forEach(producto=> { %>
                            <tr>
                                <td class="text-center">
                                    <%= producto.id %>
                                </td>
                                <td>
                                    <%= producto.nombre %>
                                </td>
                                <td class="text-center">
                                    <%= producto.clasificacion %>
                                </td>
                                <td>
                                    <%= producto.descripcion %>
                                </td>
                            </tr>
                            <% }) %>
                                <% } else { %>
                                    <tr>
                                        <td colspan="4" class="text-center">No hay productos disponibles por ahora.</td>
                                    </tr>
                                    <% } %>
                </tbody>
            </table>
        </div>
    </main>

    <!-- Footer -->
    <footer class="bg-light text-center py-3 mt-4">
        <p>&copy; <%= new Date().getFullYear() %> - Panadería Don Juanito</p>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/site.js"></script>
</body>

</html>